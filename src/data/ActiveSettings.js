const settings = {
  color: '#476797',
  brushSize: 18,
  canvasWidth: 600,
  canvasHeight: 600,
  minCanvasWidth: 50,
  minCanvasHeight: 50,
  maxCanvasWidth: 1000,
  maxCanvasHeight: 1000,
  canvasX: 0,
  canvasY: 0,
};

export { settings };
