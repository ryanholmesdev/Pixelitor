const settings = {
  color: '#476797',
  brushSize: 18,
  canvasWidth: 500,
  canvasHeight: 500,
  minCanvasWidth: 50,
  minCanvasHeight: 50,
  maxCanvasWidth: 1000,
  maxCanvasHeight: 1000,
};

export { settings };
