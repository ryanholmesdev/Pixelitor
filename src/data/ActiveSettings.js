const settings = {
  color: '#194D33',
  brushSize: 18,
  canvasWidth: 500,
  canvasHeight: 500,
  minCanvasWidth: 50,
  minCanvasHeight: 50,
  maxCanvasWidth: 1000,
  maxCanvasHeight: 1000,
};

export default settings;
